apiVersion: v1
kind: Service
metadata:
  name: {{ include "skypos-business.fullname" . }}
  labels:
    {{- include "skypos-business.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skypos-business.selectorLabels" . | nindent 4 }}
