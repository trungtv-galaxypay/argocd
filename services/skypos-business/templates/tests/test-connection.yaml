apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skypos-business.fullname" . }}-test-connection"
  labels:
    {{- include "skypos-business.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skypos-business.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
