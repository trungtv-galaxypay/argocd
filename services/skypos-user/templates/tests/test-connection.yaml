apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skypos-user.fullname" . }}-test-connection"
  labels:
    {{- include "skypos-user.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skypos-user.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
